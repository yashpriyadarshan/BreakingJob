# Edda AI Interviewer — Python Dependencies
# Install with:  uv pip install -r requirements.txt
# Or:            pip install -r requirements.txt

# ── Web framework ─────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── Pipecat (voice orchestration) ─────────────
# The [livekit] extra pulls livekit-server-sdk + livekit-rtc
# The [silero] extra pulls the Silero VAD ONNX model
pipecat-ai[livekit,deepgram,groq,cartesia]>=0.0.50

# ── LiveKit Python Server SDK ─────────────────
livekit-api>=0.7.0

# ── AI service clients ────────────────────────
deepgram-sdk>=3.5.0
groq>=0.9.0
cartesia>=1.0.0

# ── Environment & utilities ───────────────────
python-dotenv>=1.0.0
loguru>=0.7.0
pydantic>=2.0.0

# ── Azure Blob Storage ────────────────────────
azure-storage-blob>=12.28.0

# ── PostgreSQL ────────────────────────────────
psycopg2-binary>=2.9.0

# ── Report Generation ─────────────────────────
groq>=0.9.0
